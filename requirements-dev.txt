# Development & Testing dependencies
# Install with: pip install -r requirements-dev.txt
-r requirements.txt

pytest>=8.0
pytest-asyncio>=0.24
pytest-cov>=5.0
httpx>=0.27            # FastAPI TestClient (async-compatible)
freezegun>=1.5         # Freeze/travel time for JWT expiry tests
responses>=0.25        # Mock requests.get for Jikan API tests
factory-boy>=3.3       # Test data factories
