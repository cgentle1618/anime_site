// Frontend: notes configuration file for cartoonNotesConfig.
// Cartoon notes use the same section configuration as TV shows.
// Both types share identical structure, so cartoon re-exports the TV show config
// as the single source of truth. Update tvShowNotesConfig.js to change both.
export { default } from "./tvShowNotesConfig";

